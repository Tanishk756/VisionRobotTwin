# Phase B1 Implementation Plan — Read-Only ROS2 Joint State Integration

> [!NOTE]
> **Document Status**: Historical / Executed  
> **Phase**: B1 (Read-Only ROS2 JointState Integration)  
> **Original Role**: Implementation Plan  
> **Implementation Status**: Complete (Final SHA `965430e293725e68c6538f78a144aa7f48d8198b`)  
> **Current Repository State**: See [docs/status/2026-09-16-v1.3-development-summary.md](../../status/2026-09-16-v1.3-development-summary.md)

**Target Version**: 1.3.0-dev  
**Milestone**: Phase B1 — Read-Only ROS2 Joint State Backend  
**Baseline**: 214 tests passing on `develop/v1.3.0`  
**Execution Strategy**: Strict TDD, small reviewable commits, pure Python unit tests on Windows + real ROS2 integration tests on Linux.

---

## Task Decomposition Overview

- **Task 1**: Evolve `TimestampedJointState` for optional velocities & define backend state/read-only exceptions (`robotics/backends/base.py`).
- **Task 2**: Implement pure ROS-independent JointState mapping (`robotics/backends/ros2_state_mapping.py` & `tests/test_ros2_state_mapping.py`).
- **Task 3**: Implement `ROS2JointStateBackendConfig` and `ROS2JointStateDiagnostics`.
- **Task 4**: Implement optional ROS dependency boundary (`robotics/backends/ros2_joint_state_backend.py` & `tests/test_ros2_backend_optional.py`).
- **Task 5**: Implement private `rclpy.Context` / `SingleThreadedExecutor` / Node / Subscription lifecycle with fail-closed command rejection.
- **Task 6**: Implement thread-safe state cache, freshness detection (`receive_timestamp_s`), and fail-closed stale state handling (`BackendStateStaleError`).
- **Task 7**: Support `GenericRobotController` deferred validation for asynchronous state arrival.
- **Task 8**: Build standalone read-only diagnostic probe (`tools/ros2_joint_state_probe.py`).
- **Task 9**: Create Linux ROS2 Humble real integration tests (`tests/ros2/test_ros2_joint_state_backend.py`) and GitHub Actions workflow (`.github/workflows/ros2-humble.yml`).
- **Task 10**: Full verification across Windows (214 baseline + new tests) and Linux ROS2 CI.

---

## Detailed Task Specifications

### Task 1: TimestampedJointState Evolution & Backend Exceptions
- **Objective**: Evolve `TimestampedJointState` so `positions` is mandatory, `velocities` is `Optional[Tuple[float, ...]]`, and `efforts` is `Optional[Tuple[float, ...]]`. Define `BackendError`, `BackendStateUnavailableError`, `BackendStateStaleError`, `BackendStateFieldUnavailableError`, `ReadOnlyBackendError`, and `OptionalDependencyError`.
- **Files**:
  - `robotics/backends/base.py`
  - `tests/test_backends/test_backend_contracts.py`
- **TDD Workflow**:
  1. *Failing Test*: Extend `test_backend_contracts.py` to assert:
     - `TimestampedJointState` with `velocities=None` is valid.
     - `state.get_velocities_array()` raises `BackendStateFieldUnavailableError` when `velocities is None`.
     - `BackendError` hierarchy and inheritance.
  2. *Command*: `.\.venv\Scripts\python.exe -m pytest tests/test_backends/test_backend_contracts.py -v`
  3. *Expected Failure*: `AttributeError: module 'robotics.backends.base' has no attribute 'BackendStateFieldUnavailableError'`
  4. *Implementation*: Update `robotics/backends/base.py`.
  5. *Passing Test*: Re-run command -> `PASSED`.
- **Commit**: `feat(backends): support optional telemetry fields and backend state errors`

---

### Task 2: Pure ROS-Independent JointState Mapping
- **Objective**: Implement deterministic joint mapping, extra-joint pruning, malformed message rejection, and timestamp extraction with zero ROS imports.
- **Files**:
  - `robotics/backends/ros2_state_mapping.py`
  - `tests/test_ros2_state_mapping.py`
- **TDD Workflow**:
  1. *Failing Test*: Create `tests/test_ros2_state_mapping.py` testing:
     - Canonical ordering, reversed ordering, arbitrary permutations.
     - Extra joints safely ignored, missing joints rejected.
     - Duplicate incoming names rejected, empty names rejected.
     - Position-only messages accepted (`velocities=None`, `efforts=None`).
     - Full position + velocity + effort messages mapped.
     - Partial/mismatched velocity or effort array lengths rejected.
     - NaN / Inf in positions, velocities, efforts rejected.
     - Zero source timestamp preserved, receive monotonic timestamp preserved.
  2. *Command*: `.\.venv\Scripts\python.exe -m pytest tests/test_ros2_state_mapping.py -v`
  3. *Expected Failure*: `ModuleNotFoundError: No module named 'robotics.backends.ros2_state_mapping'`
  4. *Implementation*: Implement `map_joint_state_payload()` in `robotics/backends/ros2_state_mapping.py`.
  5. *Passing Test*: Re-run command -> `PASSED`.
- **Commit**: `feat(backends): add ROS-independent JointState mapping`

---

### Task 3: ROS2 Backend Configuration & Diagnostics Schemas
- **Objective**: Implement strongly-typed `ROS2JointStateBackendConfig` (defaulting to SensorDataQoS depth 5, state timeout 1.0s) and immutable `ROS2JointStateDiagnostics`.
- **Files**:
  - `robotics/backends/ros2_joint_state_backend.py` (or `ros2_state_mapping.py`)
  - `tests/test_ros2_state_mapping.py`
- **TDD Workflow**:
  1. *Failing Test*: Assert config immutability, field validation (`qos_depth >= 1`, `qos_reliability in ("best_effort", "reliable")`, `state_timeout_s > 0`, non-empty `expected_joint_names`).
  2. *Command*: `.\.venv\Scripts\python.exe -m pytest tests/test_ros2_state_mapping.py -k "test_config"`
  3. *Implementation*: Add configuration and diagnostics dataclasses.
  4. *Passing Test*: Re-run command -> `PASSED`.
- **Commit**: `feat(ros2): define ROS2 joint-state backend configuration and diagnostics`

---

### Task 4: Optional Dependency Boundary & Import Isolation
- **Objective**: Ensure `robotics/backends/ros2_joint_state_backend.py` can be imported safely without `rclpy`, and raises `OptionalDependencyError` upon construction.
- **Files**:
  - `robotics/backends/ros2_joint_state_backend.py`
  - `tests/test_ros2_backend_optional.py`
- **TDD Workflow**:
  1. *Failing Test*: Test module import when `rclpy` is absent; test constructor raises `OptionalDependencyError`.
  2. *Command*: `.\.venv\Scripts\python.exe -m pytest tests/test_ros2_backend_optional.py -v`
  3. *Implementation*: Add guarded import logic and `OptionalDependencyError` check in constructor.
  4. *Passing Test*: Re-run command -> `PASSED`.
- **Commit**: `feat(ros2): isolate ROS2 dependencies and preserve standalone Windows execution`

---

### Task 5: Private Context / Node / Executor Lifecycle & Fail-Closed Commands
- **Objective**: Implement `connect()` / `disconnect()` using a brand new `rclpy.context.Context`, node, subscription, and `SingleThreadedExecutor` on a background thread. Enforce 100% fail-closed command rejection (`ReadOnlyBackendError`).
- **Files**:
  - `robotics/backends/ros2_joint_state_backend.py`
  - `tests/test_ros2_backend_optional.py` (with mock ROS middleware)
- **TDD Workflow**:
  1. *Failing Test*: Assert command methods raise `ReadOnlyBackendError`; assert `connect()` and `disconnect()` lifecycle with mock rclpy.
  2. *Command*: `.\.venv\Scripts\python.exe -m pytest tests/test_ros2_backend_optional.py -k "test_lifecycle"`
  3. *Implementation*: Implement lifecycle and read-only fail-closed methods.
  4. *Passing Test*: Re-run command -> `PASSED`.
- **Commit**: `feat(ros2): implement private rclpy lifecycle and fail-closed read-only commands`

---

### Task 6: Thread-Safe State Cache, Freshness, & Fail-Closed Stale Handling
- **Objective**: Implement subscription callback storing immutable `TimestampedJointState` under a lock, compute `health_status()`, and raise `BackendStateStaleError` when `age > state_timeout_s`.
- **Files**:
  - `robotics/backends/ros2_joint_state_backend.py`
  - `tests/test_ros2_backend_optional.py`
- **TDD Workflow**:
  1. *Failing Test*: Test state retrieval before first message (`BackendStateUnavailableError`), fresh message (`HEALTHY`), and stale message (`BackendStateStaleError` + `DEGRADED`).
  2. *Command*: `.\.venv\Scripts\python.exe -m pytest tests/test_ros2_backend_optional.py -k "test_state"`
  3. *Implementation*: Implement mutex-protected cache and freshness evaluation.
  4. *Passing Test*: Re-run command -> `PASSED`.
- **Commit**: `feat(ros2): implement thread-safe joint-state cache and stale state protection`

---

### Task 7: GenericRobotController Deferred State Validation
- **Objective**: Allow `GenericRobotController` to construct when connected backend is waiting for state (`BackendStateUnavailableError`), while still enforcing strict validation on first available state.
- **Files**:
  - `robotics/robot_controller.py`
  - `tests/test_multi_robot_controller.py`
- **TDD Workflow**:
  1. *Failing Test*: Test controller construction with a backend that raises `BackendStateUnavailableError` on init.
  2. *Command*: `.\.venv\Scripts\python.exe -m pytest tests/test_multi_robot_controller.py -k "test_controller_deferred_backend_validation"`
  3. *Implementation*: Catch `BackendStateUnavailableError` in `_validate_initial_backend_state()`.
  4. *Passing Test*: Re-run command -> `PASSED`.
- **Commit**: `feat(controller): support deferred backend state validation for asynchronous transports`

---

### Task 8: Standalone Diagnostic Probe CLI Tool
- **Objective**: Create `tools/ros2_joint_state_probe.py` to stream live joint states and health statistics without any robot commanding capability.
- **Files**:
  - `tools/ros2_joint_state_probe.py`
  - `tests/test_tools/test_ros2_probe.py`
- **TDD Workflow**:
  1. *Failing Test*: Test argument parser (`--topic`, `--robot`, `--timeout`, `--qos`), formatting output, and read-only enforcement.
  2. *Command*: `.\.venv\Scripts\python.exe -m pytest tests/test_tools/test_ros2_probe.py -v`
  3. *Implementation*: Implement probe tool.
  4. *Passing Test*: Re-run command -> `PASSED`.
- **Commit**: `feat(tools): add read-only ROS2 joint-state diagnostic probe`

---

### Task 9: Real Linux ROS2 Humble Integration Tests & CI Workflow
- **Objective**: Add real `rclpy` integration tests (`tests/ros2/test_ros2_joint_state_backend.py`) and a dedicated GitHub Actions workflow (`.github/workflows/ros2-humble.yml`).
- **Files**:
  - `tests/ros2/test_ros2_joint_state_backend.py`
  - `.github/workflows/ros2-humble.yml`
- **TDD Workflow**:
  1. *Implementation*: Write integration tests testing real publishing/subscribing, name reordering, position-only messages, stale timeout, reconnect, and context isolation.
  2. *CI Workflow*: Configure Ubuntu 22.04 + ROS2 Humble CI job.
- **Commit**: `ci(ros2): add Ubuntu 22.04 ROS2 Humble integration tests and CI workflow`

---

### Task 10: Multi-Platform Verification & Coupling Audit
- **Objective**: Run full test suite across Windows (214 baseline + new tests) and Linux ROS2 CI, ensuring 0 regressions and 0 command capability leaks.
- **Commands**:
  - `.\.venv\Scripts\python.exe -m compileall .`
  - `.\.venv\Scripts\python.exe -m pytest -v`
  - `.\.venv\Scripts\python.exe main.py --robot panda --synthetic --headless --max-frames 120`
  - `git diff main...HEAD -- docs/experiments/v1.2_reference/` (0 diff)
